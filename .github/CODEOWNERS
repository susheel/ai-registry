# GA4GH AI Registry per-type review routing (internal project documentation Section 7,
# internal project documentation Section 6). CODEOWNERS matches bottom-to-top precedence in the
# usual GitHub sense: the LAST matching pattern in this file wins, so the
# broadest rule is listed first and the most specific overrides follow.
#
# **Both co-chair handles are now confirmed** (2026-09-04, per the person
# running the session): Marc Fiume is @mfiume; Susheel Varma is @susheel
# (cross-checked against multiple existing `github.com:susheel/...` remotes
# already configured for other repositories in this workspace, confirmed
# 2026-09-03).
#
# **Topic-reviewer carve-outs removed (2026-09-04), by explicit instruction
# from the person running the session, as part of internal project documentation Phase 9**:
# this file previously routed `data/agents/`+`data/mcp-servers/` to a
# `@REPLACE-WITH-THOMAS-CONNER-GITHUB-HANDLE` placeholder and `data/skills/`
# to a `@REPLACE-WITH-MEGAN-TSAO-GITHUB-HANDLE` placeholder, per internal project documentation
# Section 7's named intent (Thomas Conner for Project #23, Megan Tsao for
# Project #24). Neither placeholder was ever resolved to a real handle, and
# neither topic reviewer had been confirmed to have agreed to the
# responsibility (internal project documentation Section 10 item 6) -- both lines therefore had
# NO real effect regardless (GitHub silently ignores a CODEOWNERS line
# referencing an unknown username). The person running the session instructed
# removing both carve-outs outright rather than leaving inert placeholder
# lines in place. All five content types now route to the general maintainer
# group only.
#
# **This creates a fresh inconsistency with internal project documentation Section 7, left
# unresolved here for a human to reconcile, not resolved unilaterally**:
# PRD.md Section 7 still states, as written, that Thomas Conner and Megan
# Tsao hold topic-reviewer authority over their respective content types.
# This file no longer implements that. internal project documentation Phase 9's version
# history records this change and names PRD.md Section 7 as needing a
# matching update or an explicit decision to leave it as an unimplemented
# aspiration; this file does not silently edit PRD.md's own normative text.

# The two GA4GH AI Workstream co-chairs hold general maintainer authority
# across all five content types (internal project documentation Section 7). No topic-reviewer
# carve-out is active (see above).
*                    @mfiume @susheel
data/                @mfiume @susheel
